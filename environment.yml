name: qlora-analytics-agent
channels:
  - pytorch
  - nvidia
  - conda-forge
dependencies:
  - python=3.11
  - pip>=24.0
  - pytorch>=2.3.0
  - pytorch-cuda=12.1
  - pandas>=2.0.0
  - numpy>=1.26.0
  - scikit-learn>=1.4.0
  - matplotlib>=3.8.0
  - pyyaml>=6.0
  - pip:
      - transformers>=4.44.0
      - peft>=0.12.0
      - trl>=0.9.6
      - bitsandbytes>=0.43.0
      - accelerate>=0.33.0
      - datasets>=2.20.0
      - duckdb>=1.0.0
      - sqlglot>=25.0.0
      - faker>=25.0.0
      # Optional, for connecting Snowflake at work:
      # - "snowflake-connector-python[pandas]>=3.11.0"
